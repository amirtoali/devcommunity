<nav class="navbar navbar-expand-lg navbar-light bg-primary color-white">
  <div class="container-fluid">
    <a class="navbar-brand  text-white" href="<%= root_path %>">Navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse " id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active text-white" aria-current="page" href="#">Home</a>
        </li>
        <li class="nav-item">
          <% if user_signed_in? %>
           <a class="nav-link active text-white" aria-current="page" href="<%= destroy_user_session_path %>">sign out</a>
          <% else %>
           <a class="nav-link active text-white" aria-current="page" href="<%= new_user_registration_path %>">sign in</a>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>